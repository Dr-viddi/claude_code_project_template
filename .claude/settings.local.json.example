{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Per-machine overrides. Copy to settings.local.json (gitignored). Merged on top of settings.json.",
  "permissions": {
    "allow": [
      "Bash(open:*)",
      "Bash(code:*)"
    ]
  },
  "env": {
    "LOG_LEVEL": "DEBUG"
  }
}
