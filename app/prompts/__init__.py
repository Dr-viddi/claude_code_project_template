# Package marker for `app.prompts`.
#
# Prompts are versioned, named, and hot-swappable through the registry.
# Never inline prompt strings in services - look them up here. This is what
# lets you A/B test prompt changes without touching pipeline code.
