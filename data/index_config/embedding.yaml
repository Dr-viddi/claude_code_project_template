# Chunking + embedding config for scripts/seed.py. Versioned so re-indexing is
# reproducible. Used only when you wire a real vector store (VECTOR_BACKEND != memory).
model: text-embedding-3-large
dimensions: 3072
chunk_size_tokens: 512
chunk_overlap_tokens: 64
splitter: recursive
